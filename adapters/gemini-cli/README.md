# gemini-cli adapter

This directory is the declared source boundary for the native **gemini-cli** DemoSwarm
adapter. Issue #123 assigns current assets into shared, native-adapter, maintainer,
fixture, or obsolete ownership before the installable payload is populated.

The directory is intentionally not a generated translation of another host's pack.
